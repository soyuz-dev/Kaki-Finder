# Planned tests

Add meaningful behavior tests with implementation: parser examples and teaching direction; age/role independence and preference filtering; bridge ranking; Singapore date boundaries and slot overlap; interest duplicates and storage failures. Current scaffold checks: lint, typecheck, production build, and HTTP routes.
