# Interest recording

interest-button.tsx remembers a stable request ID before POST /api/interests, so a network retry cannot create another row. Fixture mode requires a browser-local save; Supabase mode persists via the validated API. The API rechecks resident compatibility and any proposed slot. A confirmation never claims to send a message or reserve a facility. Only local receipts are exposed to guests; there is no interest-listing API.
