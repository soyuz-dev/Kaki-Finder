# Database migrations

Add versioned SQL for residents, facilities, availability, and interests with the storage adapter. Enable RLS and deny public access to private records. Add ownership policies with Auth later. No schema or migrations have been applied.
